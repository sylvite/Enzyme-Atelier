# Optional public Hugging Face model token, only for exploratory ProGen2 mode.
# HF_TOKEN=

# Required only when explicitly enabling the reference-design API planner.
# Choose a model available to your API account with Responses Structured Outputs.
# OPENAI_API_KEY=
# OPENAI_MODEL=
